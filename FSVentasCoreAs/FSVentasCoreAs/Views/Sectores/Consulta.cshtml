@model IEnumerable<FSVentasCoreAs.Models.Dirreciones.Sectores>

@{
    ViewData["Title"] = "Consulta";
}

<h2>Consulta</h2>

<p>
    <a asp-action="Crear">Crear Nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nombre)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.DistritosMunicipales)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DistritosMunicipales.Nombre)
            </td>
            <td>
                <a asp-action="Editar" asp-route-id="@item.SectorId">Editar</a> |
                <a asp-action="Detalle" asp-route-id="@item.SectorId">Detalle</a> |
                <a asp-action="Eliminar" asp-route-id="@item.SectorId">Eliminar</a>
            </td>
        </tr>
}
       

</tbody>
</table>
<div>
    <a asp-action="Index">Reporte</a>
</div>
